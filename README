# Телеграм-бот для анекдотов (с парсером)

Это готовый проект телеграм-бота на `aiogram`, который рассказывает анекдоты в групповых чатах. В комплекте идет асинхронный парсер для сбора анекдотов с сайта `maximonline.ru`.

## 🚀 Возможности

- **Парсинг анекдотов**: Скрипт `parser.py` собирает 100 лучших анекдотов и сохраняет их в `jokes.json`.
- **Работа в группах**: Бот отвечает на команду `/joke` и на упоминания (@имя_бота).
- **Интеллектуальный ответ**: Распознает, когда его просят рассказать шутку, а когда просто упоминают.
- **Гибкость хранения данных**: Поддерживает работу как с `JSON` файлом, так и с базой данных `PostgreSQL`.

---

## ⚙️ Установка и запуск

Следуйте этим шагам, чтобы запустить проект.

### Шаг 1: Скачивание и установка зависимостей

1. **Сохраните код**: Скопируйте код для `bot.py` и `parser.py` в соответствующие файлы на вашем компьютере.

2. **Создайте и активируйте виртуальное окружение** (настоятельно рекомендуется):
    ```bash
    python -m venv venv
    # Для Windows
    venv\Scripts\activate
    # Для macOS/Linux
    source venv/bin/activate
    ```

3. **Установите все необходимые библиотеки**:
    ```bash
    pip install aiogram asyncpg httpx beautifulsoup4
    ```

### Шаг 2: Сбор анекдотов

Прежде чем запускать бота, ему нужна база анекдотов. Запустите парсер:

```bash
python parser.py
```

После выполнения команды в папке с проектом появится файл `jokes.json`.

### Шаг 3: Настройка и запуск бота

1. Получите токен у [@BotFather](https://t.me/BotFather) в Telegram.

2. Задайте токен как переменную окружения. Это самый безопасный способ.

    Для Windows (в командной строке):
    ```cmd
    set BOT_TOKEN=12345:ABC-DEF12345
    ```

    Для macOS/Linux:
    ```bash
    export BOT_TOKEN="12345:ABC-DEF12345"
    ```

    В качестве временного решения вы можете вписать токен прямо в код файла `bot.py`, но так делать не рекомендуется.

3. Запустите бота:
    ```bash
    python bot.py
    ```

🎉 Готово! Теперь вы можете добавить своего бота в любой групповой чат и пользоваться им.